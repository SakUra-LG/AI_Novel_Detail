# 生成脚本质量稳定改动记录

本文记录从我接手本项目到目前为止，对神话改写生成脚本所做的主要调整。目标不是单纯把字数拉长，而是在 6000-8000 字长篇生成下，让故事主线、分幕结构、文本洁净度、道具/人物一致性、幽默与感动收束都更稳定。

## 一、长篇字数与三幕结构

最早的问题是脚本容易生成短稿，或为了拉长而把动作过程写得很虚。我把长篇目标明确为 6000-8000 字，并拆成三幕目标：

- 第一幕：背景、动机、压力建立。
- 第二幕：核心行动全过程，是最长、最细的部分。
- 第三幕：结果、代价、情感收束。

同时把节拍卡数量稳定为：

- 第一幕 5-6 张。
- 第二幕 8-9 张。
- 第三幕 5-6 张。

这样做的核心作用是：字数不是靠灌水，而是靠“每个神话核心事件链被拆成可执行小节”来增加。

## 二、神话核心约束

我引入并强化了 `myth_core_constraints_revised.json` 作为每篇神话的主线约束来源。生成前会按标题/别名匹配当前神话，然后把这些内容注入规划和正文生成：

- `core_summary`：本神话的核心主旨。
- `event_chain`：必须保留的关键事件链。
- `must_include`：成稿必须出现的识别点。
- `must_not_change`：不能改变的故事方向。
- `forbidden_elements`：禁区词和禁区情节。
- `required_character_actions`：部分神话中角色必须完成的动作。

这个机制解决了两个问题：

- RAG 样本或旧稿把故事串库。
- 幽默梗抢走神话主线，导致改写不像原故事。

## 三、分幕和节拍卡解析修复

之前第三幕经常出现“只解析到 0-1 张节拍卡”的警告。根因是节拍卡解析正则把正文里的“第三幕核心节点”等普通文字也当成分隔符，提前截断了第三幕内容。

我把解析逻辑改成只识别行首的：

- `第一幕节拍卡N`
- `第二幕节拍卡N`
- `第三幕节拍卡N`
- 下一幕大纲标题

这样第三幕节拍卡不会再因为正文里出现“第三幕”二字而误切。

## 四、规划失败后的兜底方案

当模型连续生成不可用分幕或节拍卡时，我加入了基于 `event_chain` 的兜底规划。

兜底规划会按神话核心事件链自动拆成三幕和节拍卡，不追求花哨，但能保证：

- 不丢主线。
- 不乱加新势力。
- 不把辅助吐槽角色变成主角。
- 每个节拍都有明确的场景目标、画面要素、情绪推进和禁止项。

这让生成流程在模型规划不稳定时仍能继续产出可控草稿。

## 五、提示词残留硬检测

旧版本会把生成过程提示泄露进正文，比如：

- “节拍卡目标达成”
- “此处隐含感情纽带勾勒”
- “备注”
- “下一节”
- “伏笔”

我把这类词加入硬检测。只要正文或单节生成结果命中，就判为不合格并触发重写，而不是只靠后处理悄悄删除。

这保证了输出不再把写作过程说明当成小说正文。

## 六、道具、人物名称和形态一致性

针对“嫦娥奔月”里出现的道具和名字混乱问题，我没有把规则硬编码进嫦娥提示词，而是扩展了神话核心约束文件的通用字段：

- `canonical_terms`：固定名称、身份、核心道具说明。
- `forbidden_aliases`：禁止混用的错名。
- `object_consistency`：核心道具允许形态和禁止形态。

代码侧新增通用执行器，读取这些字段后统一校验。

例如嫦娥故事中：

- 固定使用“嫦娥”“蓬蒙”。
- 不死药统一为“琉璃瓶中不死药，可藏入檀木匣”。
- 禁止写成晶体、酒坛、陶罐、瓷瓶、药粉等多重形态。

这个机制以后也可以扩展到其他神话的专属道具。

## 七、重复段落和段落重启检测

长篇生成中容易出现同一功能节点反复写，比如“拿药、藏药、围观、吐槽”重复三四遍。

我加入了重复段落检测：

- 检测完全重复的段落开头。
- 检测高度相似的段落关键词重叠。
- 命中后整体质量验收失败。

同时在单节提示词中明确要求：如果前文已经写过拿出、封存、启程、抵达、吞服、离开等动作，本节不能换个说法重复，必须推进新的压力、新动作或新后果。

## 八、第三幕质量校验修正

旧的 `validate_act3` 仍按短篇目标校验，超过 1100 字就判失败；但现在长篇第三幕目标已经是 1450-1850 字。

我把第三幕长度校验改为长篇目标，避免正常长度的第三幕被误报失败。

同时第三幕仍保留：

- 繁体/乱码检测。
- 元文本污染检测。
- 格式异常检测。
- 结尾感动闭环检测。

## 九、段落可读性修复

新问题是长篇输出会出现 1000-2600 字的超长段落。原来的后处理只会处理“很长且没有句号”的段落，但很多坏段落其实有很多句号，只是没有换段。

我新增了 `split_long_paragraphs()`：

- 按句号、问号、感叹号等句子边界拆长段。
- 如果单句本身过长，再按逗号、分号等软切。
- 必要时做硬切兜底。

同时把提示词改为：

- 每个小节必须分成 2-4 个短自然段。
- 每段约 80-180 字。
- 最长不要超过 240 字。

最终验收也改严：只要成稿里仍有 360 字以上的长段，就判为不合格。

## 十、幽默机制调整

为了避免“幽默感变弱”或“每篇都像同一个角色在吐槽”，我调整了 `humor_punchline_examples.txt` 和调用逻辑。

现在笑点库支持：

- `【适用神话：xxx】` 标签。
- 每个神话至少有对应 punchline 示例。
- 生成时优先加载当前神话专属示例。
- 再补少量通用示例。

代码侧也调整为：

- 不再把所有 punchline 一次性塞给模型。
- 专属样本只作为节奏参考，不要求照抄。
- 生成 punchline 时要求围绕当前节拍的动作、道具、身体感受或压力制造笑点。
- 关键转折、牺牲、分离、死亡、惩罚等节拍自动减少或跳过强笑点。

目标是恢复“读到能笑出来”的对白强度，同时不破坏神话主线和结尾情绪。

## 十一、当前整体效果

现在生成脚本的稳定性主要依赖四层防线：

1. 生成前：神话核心约束控制方向。
2. 规划时：三幕和节拍卡确保长篇结构。
3. 逐节生成时：单节校验、污染检测、重复约束和专属 punchline 控制质量。
4. 成稿后：最终清洁、分段、主线一致性、道具一致性、长段检测和整体质量验收。

这些改动共同服务一个目标：让脚本可以写长，但不是靠堆字；可以搞笑，但不串戏；可以感动，但不靠提示词残留或硬煽情。
