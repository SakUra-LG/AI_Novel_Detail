# 《山海二十简》神话小说改写流程与字数扩充交接文档

> 更新时间：2026-08-14  
> 适用项目：`AI_Novel_Detail1`  
> 主要用途：把当前二十篇神话改写的实际文件、世界观、生成流程、人工修改方法、Qwen 调用方式、审计规则和字数扩充策略移交给下一段对话。  
> 当前后续任务重点：在不破坏既有情节、语言风格和阿满串线设定的前提下，对篇幅不足或需要加强的正文做定点扩充。

## 1. 下一段对话应先知道的结论

1. 最终可交付正文只认下面这个目录：

   `D:\Study\College\Scientific research\小说细节\小说细节\细节生成资料\AI_Novel_Detail1\Detail\outputs\20个神话故事（逐篇重制版）\texts`

2. 该目录中应有 **20 篇故事正文 + 1 篇总序**。`00_总序.txt` 不算一篇神话，不应计入“20篇”的篇数或单篇字数。
3. 系列书名已经统一为 **《山海二十简》**，二十篇地位相同，不再使用“前十八简”“增补简”“封卷后追加”等设定。
4. 阿满的完整身份统一为 **“不受凡间年代限制的瑶池见闻小史官”**。他按“神话次序”而非历史次序行走。
5. 后续只做字数扩充时，优先人工定点扩写；不要为补几十或几百字整篇重跑模型。
6. 如确需使用 Qwen，先在 `work` 或单独候选目录生成，人工通读后才能覆盖 `texts`。不要让生成脚本直接改最终稿。
7. 任何自动审计结果都只是定位线索，不可代替人工阅读。当前批量静态审计存在严格关键词误报，不能看到 `pass=False` 就整篇重写。

## 2. 项目关键目录和权威层级

### 2.1 最终文本

最终目录：

`Detail/outputs/20个神话故事（逐篇重制版）/texts/`

目录内包括：

- `00_总序.txt`
- `八仙过海.txt`
- `北冥鲲鹏.txt`
- `仓颉造字.txt`
- `嫦娥奔月.txt`
- `大禹治水.txt`
- `伏羲画卦.txt`
- `后羿射日.txt`
- `精卫填海.txt`
- `夸父追日.txt`
- `雷泽华胥.txt`
- `梁山伯与祝英台.txt`
- `孟姜女哭长城.txt`
- `哪吒闹海.txt`
- `牛郎织女.txt`
- `女娲补天.txt`
- `女娲造人.txt`
- `神农尝百草.txt`
- `吴刚伐桂.txt`
- `西王母.txt`
- `愚公移山.txt`

修改优先级必须遵循：

1. `texts/*.txt`：当前人工认可的最终正文，是最高权威。
2. `knowledgeBase/myth_core_constraints_revised.json`：二十篇不可改的神话核心事件和人物动作。
3. `knowledgeBase/myth_thread_protagonist_constraints.json`：阿满身份、道具、幽默、跨篇串线和时序规则。
4. `knowledgeBase/myth_chapter_plans_rebuild.json`：八章事件表，目前只覆盖17篇，主要用于重制或较大幅度扩写。
5. `main.py`：通用生成、清洗、修复、长度控制、污染检测、RAG 和 Qwen API 调用。
6. `work/`、`reports/`、`logs/`：候选、审计与过程文件，不能反过来覆盖人工认可正文。

历史输出目录、旧报告、退回候选和缓存只可参考，不能当作最终稿来源。

### 2.2 三个核心约束文件

#### `myth_core_constraints_revised.json`

当前覆盖20篇。每篇主要包含：

- 核心主旨；
- 必须保留的事件链；
- 必须出现的识别点；
- 角色必须亲自完成的动作；
- 不能改变的结局与寓意；
- 禁止新增或混用的角色、道具、设定；
- 个别故事的固定称谓和物件形态。

扩写前必须先读目标篇对应对象。扩写只能增加过程、阻力、选择、反应和余波，不能改变该文件锁定的事件顺序与归属。

#### `myth_thread_protagonist_constraints.json`

当前覆盖20篇，是阿满串线的权威来源。核心规则如下：

- 完整身份：不受凡间年代限制的瑶池见闻小史官；
- 固定道具：青竹简、总掉毛的旧笔、小布袋；《西王母》还交代青色系带的来处；
- 行走方式：空白青简按“神话次序”把他引到不同年代；
- “上回”“以后”“下一简”只表示阿满个人记录顺序，不表示历史年代先后；
- 他能见证、记录、护简、类比、吐槽和短促回忆，不能预知结局；
- 他不能施法、指挥、递关键道具、替主角决定或完成核心行动；
- 其他神话人物不能实体进入当前篇帮忙；
- 跨篇连接通常只用一两句，最好通过翻简、想起旧经历或页角补记完成；
- 重大死亡、牺牲、分离、担责和惩罚场面必须及时收住笑点。

#### `myth_chapter_plans_rebuild.json`

这是人工锁定的八章推进表。每章通常包含：

- `function`：本章叙事功能；
- `goal`：当前人物目标；
- `obstacle`：具体阻碍；
- `adjustment`：人物采取的新行动；
- `result`：章末不可逆的新状态；
- `core_nodes`：本章必须现场演出的经典节点；
- `required_terms` / `forbidden_terms`；
- `aman_mode` / `aman_max_mentions`；
- `target_min` / `target_max` 和本地硬长度范围。

重要现状：该文件目前只有17篇，缺少：

- `后羿射日`
- `哪吒闹海`
- `大禹治水`

这三篇在 `rebuild_myths_chaptered.py` 中被列为 `DIRECTLY_ACCEPTED`。因此，不能直接把这三篇交给八章重制脚本，否则会因找不到正式计划而失败。它们若只差少量字数，应人工扩充；若未来要整篇重制，必须先生成计划草案、人工审核，再将计划正式并入 JSON。

### 2.3 人工样本文件

以下文件会影响幽默和情感风格：

- `Detail/knowledgeBase/humor_punchline_examples.txt`
- `Detail/knowledgeBase/touching_foreshadow_examples.txt`
- `Detail/knowledgeBase/touching_ending_examples.txt`
- `Detail/knowledgeBase/themes_Content.json`

`humor_punchline_examples.txt` 中有按神话标注的人工笑点样本。新增笑点时应学习其机制，不得逐句照抄。最有效的笑点通常来自：

- 阿满怕热、怕水、怕高、怕累却嘴硬；
- 他护青竹简时手忙脚乱；
- 他认真写错一个字，被人物当场拆台；
- 主角或百姓用一句朴素的话戳破他的体面；
- 当前神话道具与阿满的小狼狈形成反差。

## 3. 《山海二十简》的世界观口径

总序位于：

`Detail/outputs/20个神话故事（逐篇重制版）/texts/00_总序.txt`

总序已经解决跨时代矛盾，后续正文或扩写不得破坏：

1. 瑶池青简不记凡间年号，只记神话事件。
2. 阿满跨越远古、上古、民间传说及不同历史层次，不需要解释成现代意义上的穿越。
3. 《西王母》记录的是西王母授命及信物的来处，但该篇在书中的位置不等于历史第一篇。
4. 阿满能记得自己已经去过的故事，也能说“以后若轮到某简”，但这只是记录旅程，不是历史预言。
5. 阿满不知道正在发生的故事最终会怎样，不能提前说破结局。
6. “见闻小史官”“瑶池小史官”“小史官”可以是同一身份的简称；需要完整介绍时统一写“瑶池见闻小史官”。
7. 二十篇都是《山海二十简》的正式篇目，哪吒与大禹不得再写成增补篇。

## 4. 当前生成流程的实际结构

### 4.1 `main.py` 的通用旧流程

入口函数为 `generate_myth_rewrite(prompt)`。主要过程是：

1. 根据标题或别名匹配 `myth_core_constraints_revised.json`；
2. 同时挂载该篇的阿满串线约束；
3. 读取 RAG 内容及人工幽默/感动样本；
4. 生成总体大纲；
5. 拆为三幕和节拍卡；
6. 分幕生成并进行单幕校验；
7. 合并正文；
8. 执行语言清理、分段、事件链检查、阿满越权检查、重复检测和字数修复；
9. 必要时进入受控重写或终审扩写。

当前长度常量在 `Detail/main.py` 顶部：

```python
MYTH_TARGET_TOTAL_MIN = 7500
MYTH_TARGET_TOTAL_SOFT_MAX = 8700
MYTH_TARGET_TOTAL_MAX = 9300
```

这套流程功能很多，但历史逻辑较长，包含多层兜底和尾部补强。若只是修改现有20篇，不建议从这里整篇重跑，因为可能改变已认可的情节表达、笑点分布和阿满出场位置。

### 4.2 推荐的八章重制流程

脚本：`Detail/rebuild_myths_chaptered.py`

适用场景：

- 某篇结构性问题较大；
- 需要按照八章事件表重新生成；
- 不只是补几十或几百字；
- 目标篇已经存在于 `myth_chapter_plans_rebuild.json`。

核心机制：

1. 每章按目标、阻碍、调整、结果生成；
2. 每章检查篇幅、必须词、禁词、阿满次数和跨章重复；
3. 八章合并后进行本地全文审计；
4. 调用 Qwen 做结构化审稿；
5. 按问题定向重写相关章节；
6. 通过自动门槛后只生成候选；
7. 只有显式加 `--promote` 才写入最终 `texts`。

本地全文长度硬门槛为去掉空白后的 **7500—8850字符**。这比 `main.py` 的9300硬上限更严格，因此使用八章流程时建议最终控制在7500—8700之间。

### 4.3 四章区块候选流程

脚本：`Detail/generate_myth_in_blocks.py`

它把八章拆为“第1—4章”和“第5—8章”两个连续区块生成，目的是减少逐章生成造成的开头重复和拼接感。产物只进入：

- `work/<故事名>/chapters/`
- `work/<故事名>/block_candidates/`
- `work/<故事名>/block_assembled_candidate.txt`
- `work/<故事名>/block_generation_audit.json`

该脚本不会自动把候选写入 `texts`，适合先做大段候选，再人工逐章阅读。

### 4.4 八章计划草案流程

脚本：`Detail/draft_rebuild_chapter_plan.py`

它只生成计划草案，默认输出到：

`Detail/outputs/20个神话故事（逐篇重制版）/plan_drafts/`

草案必须人工检查以下内容后才能并入正式计划：

- 八章是否真正依次推进，而非同一事件换词重复；
- 每章是否有新目标、新阻碍、新选择、新结果；
- 核心神话节点是否归属于正确人物；
- 阿满是否只在少数必要章节出现；
- 结局是否在现场完成，不靠第八章总结前七章；
- 每章目标字数能否支撑全文7500字以上。

### 4.5 批量生成与审计脚本

脚本：`Detail/batch_myth_quality_audit.py`

它支持：

- 批量生成指定故事；
- 仅审计现有正文；
- 可选 Qwen 主观审稿；
- 生成单篇 JSON 报告和总 `summary.json`。

安全警告：如果把 `--output-dir` 指向最终目录，却没有加 `--skip-generate`，脚本会直接生成并写入 `texts/<故事名>.txt`。对当前已认可正文做检查时，必须使用：

```powershell
python "Detail\batch_myth_quality_audit.py" `
  --skip-generate `
  --no-preflight `
  --output-dir "Detail\outputs\20个神话故事（逐篇重制版）"
```

这条命令不调用 Qwen，也不改正文，只会更新 `reports` 和 `summary.json`。导入 `main.py` 较慢，约几十秒无输出不一定是卡死。

## 5. Qwen API 配置与调用

项目当前从 `Detail/config.py` 导入 `API_Key_QW`。该文件含敏感凭证：

- 不要把密钥写进本交接文档；
- 不要在终端输出其内容；
- 不要提交到公开仓库；
- 下一段对话可直接调用现有配置，无需询问用户重新提供密钥。

默认长篇模型由环境变量控制：

```text
QWEN_GENERATION_MODEL=qwen-plus
```

相关环境变量：

- `QWEN_GENERATION_MODEL`：生成模型，默认 `qwen-plus`；
- `QWEN_API_TRANSPORT`：`auto`、`sdk` 或 `curl`；
- `QWEN_API_TIMEOUT_SECONDS`：单次调用超时；
- `QWEN_API_MAX_RETRIES`：重试次数；
- `QWEN_CURL_INTERFACE`：必要时绑定网卡；
- `QWEN_CURL_RESOLVE_IP`：必要时指定 DashScope 域名解析 IP。

`main.py` 中统一使用 `call_qianwen_api(...)`，并支持 SDK 与 `curl.exe` 两种通道。正常情况下推荐脚本默认的 `curl`，模型用 `qwen-plus`。不要为了补少量字数调用更高温度整篇重写。

## 6. 后续“字数扩充”的推荐方法

### 6.1 先统一字数口径

不同脚本的计数略有差异：

- `main.py` 和批量审计常直接使用 `len(text)`，会计入换行；
- 八章重制脚本的 `count_chars()` 会删除所有空白后再计数；
- 导师说的“7500字左右”更适合用“去掉空白后的字符数”作为保守口径。

推荐交付门槛：

- 最低：去空白后不少于7500字符；
- 理想：7800—8500字符；
- 建议软上限：8700字符；
- 使用八章重制流程时，绝对不要超过8850字符；
- 不要为了凑到整数机械补尾。

### 6.2 当前二十篇去空白字符数

以下数据是交接时对最终 `texts` 目录的实测结果：

| 故事 | 去空白字符数 | 当前判断 |
|---|---:|---|
| 八仙过海 | 7891 | 达标 |
| 北冥鲲鹏 | 8191 | 达标 |
| 仓颉造字 | 8516 | 达标，接近建议上段 |
| 嫦娥奔月 | 7562 | 达标但余量较小 |
| 大禹治水 | 7432 | 少约68字，人工微扩即可 |
| 伏羲画卦 | 7621 | 达标但余量较小 |
| 后羿射日 | 6949 | 明显不足，第一优先扩充 |
| 精卫填海 | 7934 | 达标 |
| 夸父追日 | 7757 | 达标 |
| 雷泽华胥 | 7741 | 达标 |
| 梁山伯与祝英台 | 8397 | 达标 |
| 孟姜女哭长城 | 8082 | 达标 |
| 哪吒闹海 | 7452 | 少约48字，人工微扩即可 |
| 牛郎织女 | 8476 | 达标，接近建议上段 |
| 女娲补天 | 7723 | 达标 |
| 女娲造人 | 8329 | 达标 |
| 神农尝百草 | 8218 | 达标 |
| 吴刚伐桂 | 7776 | 达标 |
| 西王母 | 8477 | 达标，接近建议上段 |
| 愚公移山 | 8700 | 已到建议软上限，不再扩充 |

推荐扩充顺序：

1. `后羿射日`：补约600—900字，目标去空白后7550—7850；
2. `大禹治水`：补约120—250字，给统计差异留余量；
3. `哪吒闹海`：补约120—250字，给统计差异留余量；
4. 其余篇只有在导师明确要求统一更长时再处理，不要全体机械加字。

### 6.3 最安全的人工定点扩充法

对现有最终稿扩字，按以下步骤执行：

1. 阅读整篇和该篇核心约束，列出事件链与当前段落功能。
2. 找到“已经发生转折，但动作、阻力或人物反应略快”的位置。
3. 在原段内部或相邻段之间补一个微场景，不在结尾另贴总结段。
4. 每个新增微场景只完成一件事，例如：
   - 主角为下一核心动作做具体准备；
   - 环境造成一个与主线直接相关的新阻碍；
   - 配角对刚发生的结果作出行动反应；
   - 主角在两个选择之间做出决定；
   - 前一动作留下可见后果，推动下一事件；
   - 阿满贡献一轮短促对话后退回背景。
5. 新增内容必须承接前文状态，并准确交给后文原有首句，不能造成时间、位置、物件状态突然跳变。
6. 补完后重读新增段前后各两段，删除重复解释和同义复述。
7. 重新统计去空白字符，检查引号、书名和元文本污染。

扩写长度建议：

- 缺50—150字：扩充一处动作与一句自然对白；
- 缺150—350字：增加一个完整的微场景；
- 缺350—700字：在两个不同事件节点各扩一处；
- 缺700字以上：先画出两到三个扩写位置，避免把全部字数塞在结尾。

### 6.4 后羿射日的定点扩充建议

`后羿射日` 当前去空白约6949字符，是唯一明显低于门槛的故事。它已经是项目认可的风格基准之一，禁止整篇推倒重写。建议在不改变“十日并出—百姓受难—后羿逐箭射日—射九留一—灾后恢复”的前提下，选择两处扩写：

1. 射日前的准备与民生压力：
   - 写百姓如何把仅存的水、阴影和箭羽材料交给后羿；
   - 写后羿检查弓、落脚点、风势时身体承受的热浪；
   - 阿满可因竹简发烫、字被汗洇开贡献短笑点；
   - 不能让阿满提出射日办法。

2. 射日过程中的递进与代价：
   - 每一箭不必机械计数，但可写太阳减少后光热、风、鸟兽和百姓反应逐步变化；
   - 后羿的手臂、呼吸和判断逐渐吃力，但不要写技术化肌肉结构或血肉伤口；
   - 必须保留“射九留一”的主动判断，不能写成箭用完或偶然剩一日。

3. 灾后余波可小幅补充：
   - 第一口重新烧开的水、第一锅粥、种子和牲畜如何被重新安置；
   - 只补具体生活恢复，不再总结一遍英雄功绩。

总增量建议约650—800字，分散到两处或三处。

### 6.5 大禹与哪吒的微扩建议

`大禹治水` 与 `哪吒闹海` 只略低于去空白7500字符，不值得调用模型整篇重写。

大禹可补：

- 一小段洪水退后，乡民清沟、扶正田界、试着重新引水；
- 大禹确认水路已经通畅后才转向家门；
- 阿满护住沾泥竹简的一句短对白。

不得补：新的治水神兽、阿满提出“改堵为疏”、再次重演三过家门、结尾再列一遍全部功绩。

哪吒可补：

- 陈塘关水退后，百姓修复鱼架、门槛或灶火；
- 哪吒用普通行动帮助收拾残局，体现重生后的担当；
- 李靖、殷夫人或百姓与哪吒的一轮短对白；
- 阿满最后护简的笑点可保留，但不要延长赴死场面的玩笑。

不得补：新战斗、龙王再次来袭、阿满参与谈判、太乙真人新增法宝或哪吒再次担责一次。

### 6.6 使用 Qwen 做扩写时的局部提示原则

如果人工扩写困难，仍应让模型只写“插入段候选”，不要给它整篇重写权限。提示中必须提供：

- 故事标题；
- 插入位置之前的末段；
- 插入位置之后的首段；
- 当前已完成的事件；
- 本段只允许完成的新动作；
- 目标字数；
- 不能改变的章末状态；
- 阿满是否允许出现；
- 禁止元文本、重复开场、总结补字和新增神异元素。

可复用的局部提示模板：

```text
请只写一段可插入《故事名》现有正文的连续小说正文，目标约___个中文字符。

前文末段：
<前文>
……
</前文>

后文首段：
<后文>
……
</后文>

当前确定状态：……
本段任务：通过“具体目标—现实阻碍—人物选择—行动—直接后果”扩充……，并自然承接后文。
阿满规则：本段允许/不允许阿满出现；若出现，只能记录、护简和贡献一轮短笑点，不能解决问题。
硬禁区：不得重演前文，不得提前完成后文，不得新增神仙、神兽、法宝或支线，不得写章节、读者、作者、审核、预告、重复、字数、任务等元文本。
只输出插入段正文，不输出说明。
```

模型返回后必须人工改写衔接词，再插入最终稿。不要原样粘贴后立即交付。

## 7. 推荐命令

以下命令都应在项目根目录运行：

`D:\Study\College\Scientific research\小说细节\小说细节\细节生成资料\AI_Novel_Detail1`

### 7.1 Python 和 JSON 基础校验

```powershell
python -m py_compile `
  "Detail\main.py" `
  "Detail\batch_myth_quality_audit.py" `
  "Detail\rebuild_myths_chaptered.py" `
  "Detail\generate_myth_in_blocks.py" `
  "Detail\draft_rebuild_chapter_plan.py"
```

```powershell
@'
import json
from pathlib import Path
for name in (
    "myth_core_constraints_revised.json",
    "myth_thread_protagonist_constraints.json",
    "myth_chapter_plans_rebuild.json",
):
    path = Path("Detail/knowledgeBase") / name
    json.loads(path.read_text(encoding="utf-8"))
    print("JSON OK:", path)
'@ | python -X utf8 -
```

### 7.2 统计最终正文

```powershell
$base = "Detail\outputs\20个神话故事（逐篇重制版）\texts"
Get-ChildItem -LiteralPath $base -Filter "*.txt" |
  Where-Object Name -ne "00_总序.txt" |
  Sort-Object Name |
  ForEach-Object {
    $text = [IO.File]::ReadAllText($_.FullName)
    [pscustomobject]@{
      故事 = $_.BaseName
      去空白字符 = [regex]::Replace($text, "\s+", "").Length
      左引号 = ([regex]::Matches($text, "“")).Count
      右引号 = ([regex]::Matches($text, "”")).Count
      有阿满 = $text.Contains("阿满")
      有山海二十简 = $text.Contains("山海二十简")
    }
  } | Format-Table -AutoSize
```

### 7.3 搜索旧世界观和元文本污染

```powershell
$base = "Detail\outputs\20个神话故事（逐篇重制版）\texts"
rg -n "山海十八简|前十八简|增补简|封卷之后|未来一简|瑶池小史官" -- $base
```

总序中为了说明简称而出现一次“瑶池小史官”是允许的；正文中应优先使用完整称谓。

```powershell
rg -n "这句话不是预告|不是重新开场|不是后来.*重复|按发生顺序完整|不由结尾一句替代|审稿|审核者|验收|创作说明|任务要求|字数要求|提示词|模型生成" -- $base
```

### 7.4 只做本地批量审计

```powershell
python "Detail\batch_myth_quality_audit.py" `
  --skip-generate `
  --no-preflight `
  --output-dir "Detail\outputs\20个神话故事（逐篇重制版）"
```

### 7.5 为已有正式八章计划的故事生成候选

先生成，不提升到最终稿：

```powershell
python "Detail\rebuild_myths_chaptered.py" `
  --stories "夸父追日" `
  --resume `
  --max-revision-rounds 2 `
  --api-transport curl `
  --model qwen-plus
```

候选通过后应阅读：

- `Detail/outputs/20个神话故事（逐篇重制版）/work/夸父追日/automated_gate_candidate.txt`
- 对应 `reports/夸父追日.json`
- 八个 `work/夸父追日/chapters/*.txt`

只有人工通读确认后，才允许：

```powershell
python "Detail\rebuild_myths_chaptered.py" `
  --stories "夸父追日" `
  --resume `
  --reuse-passed-audit `
  --promote `
  --api-transport curl `
  --model qwen-plus
```

不要对 `后羿射日`、`哪吒闹海`、`大禹治水`直接使用上述命令，因为当前正式八章计划中没有这三篇。

### 7.6 四章连续区块候选

```powershell
python "Detail\generate_myth_in_blocks.py" `
  --stories "夸父追日" `
  --model qwen-plus `
  --api-transport curl
```

它只生成候选，不会提升到 `texts`。

## 8. 自动审计的已知局限

最近一次对最终目录运行 `batch_myth_quality_audit.py --skip-generate --no-preflight` 后，`summary.json` 显示仅4篇静态全通过。这个数字不能直接解释为“只有4篇可用”，原因包括：

1. 审计器要求每篇逐字出现“山海二十简”，而当前部分已交付正文只有阿满和青竹简，没有显式书名；
2. 跨篇连接检查依赖固定词表和固定桥接标记，自然表达可能被误判；
3. `humor_signal_count` 依赖“笑、嘀咕”等表面标记，纯对白反差可能被低估；
4. 某些禁词检测只按字符串命中，可能不了解上下文；
5. 批量审计的“新增两篇”分类仍保留历史命名，只是用来给哪吒、大禹设置更高幽默门槛，不代表它们仍是增补简；
6. 报告会因人工正文修改而失去哈希一致性，旧报告不能证明新正文已经过同一轮审稿。

因此应把失败项分成两类：

- **真实硬问题**：字数不足、引号不配对、乱码、提示词进入正文、情节缺失、阿满越权、结局改变、重复整段；必须修。
- **规则误报或口径问题**：未逐字出现某个固定短语、自然笑点未命中标记、桥接句未被词表识别；人工判断后再决定是否改。

当前确实需要处理的字数硬问题主要是后羿，其次是哪吒与大禹的少量安全余量。

## 9. 正文质量硬规则

### 9.1 情节

- 原神话核心人物必须亲自完成核心行动；
- 经典节点必须在现场演出，不能只在结尾列举；
- 不允许新增神仙、神兽、法宝、预言、身世或奇迹解决危机；
- 不允许为了扩字重演已经发生的事件；
- 每一新增段都必须产生状态变化；
- 结局和寓意不能改变。

### 9.2 阿满

- 男性，代词使用“他”；
- 完整身份为瑶池见闻小史官；
- 固定携带青竹简、旧笔、小布袋；
- 不得新增地图、日晷、墨瓶、法宝化竹简等道具；
- 主要作用是记录、观察、护简、吐槽、情绪见证和跨篇连接；
- 不得替主角找方法、作决定、谈判、施法或完成行动；
- 出场要有功能，不要每段都重复介绍；
- 同一篇中不要反复使用摔倒、掉笔、抱紧竹简这一种笑法。

### 9.3 幽默

- 要有明显幽默感，但必须从人物关系和当前动作里长出来；
- 优先使用短对白、互怼、误写字、嘴硬和处境反差；
- 不使用网络梗、现代职场梗、直播综艺、系统面板和低俗身体笑料；
- 不把灾民、死亡、牺牲、分离和重大痛苦当笑料；
- 重大转折后可暂时完全停止笑点；
- 笑点不是数量清单，不要在正文中写“第几个笑点”。

### 9.4 语言与元文本

正文不得出现向审核者证明合规的句子，例如：

- “这句话不是预告。”
- “这不是后来……的重复。”
- “这个转向不是重新开场。”
- “从抵达到诞生，所见事实按发生顺序完整留在竹简上。”
- “每一件事都在现场完成，不由结尾一句替代。”

正确做法是直接写人物行动与现场结果，让读者自然理解没有重复、没有跳步。不要在正文中出现“读者、作者、模型、提示词、任务、章节安排、验收、字数统计”等创作流程词。

### 9.5 结构与段落

- 最终稿不保留章节标题、幕名、计划标签和 Markdown；
- 段落以具体动作或对白推进，避免连续大段总结；
- 不在每章或每个事件后重复点题；
- 不用精确尺寸、角度、时辰、材料清单、身体机理或操作说明凑字；
- 不细写骨骼、肌肉、伤口、流血和痛苦机理；
- 中文引号必须成对。

## 10. 安全修改与交付步骤

每次只处理一篇，遵循以下顺序：

1. 记录修改前的去空白字符数。
2. 阅读该篇完整正文、核心约束、阿满约束；有八章计划时一并阅读。
3. 标出要扩写的位置和预计增量。
4. 先在候选文件或补丁中修改，不使用批量全局替换处理剧情句。
5. 对比修改前后，确认没有删除既有事件、笑点、关键称谓和结局。
6. 搜索旧书名、增补简、元文本和现代词。
7. 检查中文引号配对和去空白字符数。
8. 人工连续阅读新增段前后至少两段，检查衔接。
9. 如用了模型，保存提示、候选和审稿记录到 `work`，不要直接覆盖最终稿。
10. 只有人工确认后才将候选写入 `texts`。

项目当前工作区不是干净状态，很多既有改动和未跟踪文件属于前序工作。不要执行：

- `git reset --hard`
- `git checkout -- .`
- 删除整个 `outputs`、`work` 或项目目录
- 对所有正文做无差别格式化
- 未确认目标路径的递归移动或删除

只修改任务明确涉及的文件，并用 `git diff -- <文件>` 查看差异。

## 11. 交接时的当前状态

已完成：

- 最终目录已有20篇正文；
- 新增 `00_总序.txt`；
- 书名统一为《山海二十简》；
- 阿满世界观已补入总序、串线约束和主要生成提示；
- 哪吒、大禹不再按增补简处理；
- 导师点名的七篇中，明显的“审核规则进入正文”句已人工改写；
- 其他篇发现的少量同类元叙事也做了最小清理；
- `main.py` 已增加对典型审稿口吻的污染检测；
- Python 主要脚本可通过 `py_compile`，三个 JSON 可正常解析。

仍建议下一段对话优先做：

1. 人工扩充 `后羿射日` 至去空白7500以上；
2. 给 `大禹治水`、`哪吒闹海` 各补少量自然正文，留出统计余量；
3. 扩充后再次全量搜索元文本、旧书名和“增补简”；
4. 对20篇做一次人工串线复核，尤其检查那些正文中未逐字出现“山海二十简”的篇目是否需要以最小句子补齐；
5. 不要因旧 `summary.json` 的低通过数整批重写。

## 12. 给下一段对话的建议开场指令

可把下面内容直接交给下一段对话：

```text
请先完整阅读项目根目录的《神话小说改写流程与字数扩充交接文档.md》，再读取：
1. Detail/outputs/20个神话故事（逐篇重制版）/texts/00_总序.txt
2. 目标故事最终正文
3. Detail/knowledgeBase/myth_core_constraints_revised.json 中目标故事条目
4. Detail/knowledgeBase/myth_thread_protagonist_constraints.json 的全局规则与目标故事条目
5. 如果存在，再读取 Detail/knowledgeBase/myth_chapter_plans_rebuild.json 的目标故事计划

本轮先处理字数扩充。最终正文目录是 Detail/outputs/20个神话故事（逐篇重制版）/texts。
优先把《后羿射日》从去空白约6949字符扩充到7550—7850；只做定点扩写，不改变事件链、结局、现有主要笑点和阿满身份。扩写要进入原有动作过程，不得在结尾追加总结，不得使用审稿/验收口吻。完成后检查去空白字数、中文引号、旧书名、增补简和元文本污染，并报告具体插入位置和最终字数。
```

---

本交接文档描述的是当前项目的真实状态。若后续代码或约束发生变化，应先更新本文中的目录、篇数、长度门槛、计划覆盖范围和命令，再继续向下一段对话移交。
